#include <iostream>
#include <cassert>

using namespace std;

int main()
{
    double quotient, numerator = 5, denominator = 0;

    assert(denominator);
    quotient = numerator / denominator;

    cout << quotient;

    return 0;
}